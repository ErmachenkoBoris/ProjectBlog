import { ReadAccessPipe } from './read-access.pipe';

describe('ReadAccessPipe', () => {
  it('create an instance', () => {
    const pipe = new ReadAccessPipe();
    expect(pipe).toBeTruthy();
  });
});
